Place the signed universal Swara APK here as:

Swara.apk

The local distribution server serves this asset at /app.apk before falling back to
the currently installed APK. Use a universal APK from the release/debug build
pipeline, not a split APK or extracted base.apk.
